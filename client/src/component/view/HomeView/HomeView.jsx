import React from 'react';

// Import components =============================================================
import Button from 'component/base/Button';

const HomeView = () => (
  <Button onClick={() => {}}>
    Login With Google
  </Button>
);

export default HomeView;
